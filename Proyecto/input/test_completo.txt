int factorial(int n);

int x = 2;
double pi = 3.14;

int suma(int a, int b) {
    int resultado;
    resultado = a + b;
    return resultado;
}

int factorial(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

int main() {
    int i = 0;
    int sum = 0;
    int fact = 0;

    while (i < 5) {
        i = i + 1;
        if (i == 3) {
            continue;
        }
        sum = suma(sum, i);
    }

    fact = factorial(x);

    int r = suma(fact, 10);
    return r;
}
